import styled from 'styled-components'
import Grid from '@mui/material/Grid'

export const Wrapper = styled(Grid)`
    height: 100vh;
    overflow: hidden;
`

export const ContentWrapper = styled(Grid)`
    height: 100vh;
    overflow-y: scroll;
    padding-bottom: 40px;
`

export const SidebarHolder = styled(Grid)`
    height: 100vh
`